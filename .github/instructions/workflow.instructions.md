---
description: Repository workflow preferences for completing validated changes
---

- After completing and validating a small, self-contained change, commit it automatically instead of leaving a dirty worktree for a separate commit request.
- After committing a small, validated change, push it automatically, wait for the GitHub Pages deployment, and verify the live site serves the new build.
